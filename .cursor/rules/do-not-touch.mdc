---
description: "Protected files that should not be modified"
globs: "packages/excalidraw/scene/renderer.ts,packages/excalidraw/data/restore.ts,packages/excalidraw/actions/manager.ts,packages/excalidraw/types.ts"
alwaysApply: true
---

# Protected Files

NEVER modify these files without explicit approval:

- `packages/excalidraw/scene/renderer.ts` — render pipeline
- `packages/excalidraw/data/restore.ts` — file format compat
- `packages/excalidraw/actions/manager.ts` — action system
- `packages/excalidraw/types.ts` — core types

Changes to protected files require:

1. Full understanding of dependencies
2. Running complete test suite
3. Manual QA verification

## How to verify

- `git diff --name-only` — check that none of the protected files appear in changes
- `yarn test:all` — full suite must pass before touching these files
- Review all imports of the changed file: `grep -r "from.*renderer\|from.*restore\|from.*manager\|from.*types" packages/`